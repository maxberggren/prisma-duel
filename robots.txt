# Prisma Duel. The game and its pages are meant to be found.
User-agent: *
Allow: /

# Room state and health, useful to nobody but the operator.
Disallow: /rooms.json
Disallow: /healthz
Disallow: /__net/
# The pages' unassembled sources; the assembled pages answer at clean URLs.
Disallow: /pages/src/

# server.js rewrites this origin to whatever host actually served the file,
# so a preview deployment does not advertise production's sitemap.
Sitemap: https://prismaduel.com/sitemap.xml
